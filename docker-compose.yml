services:
  app:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    env_file: .env
    environment:
      NODE_ENV: production
      PORT: "3000"
      DATABASE_PATH: /app/data/db.sqlite
      UPLOAD_DIR: /app/data/uploads
    volumes:
      - bsp_data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/api/v1/public/status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  bsp_data:
    driver: local
